/*
全称量词

题目描述
已知客体域A为“大于等于2、小于等于10000的自然数”，谓词P(x, y)表示“x和y互质(x≠y)”。

问题1：对输入的客体常量a，如果希望谓词公式(∀y)P(a, y)永为T，则客体变量y的取值范围最多可以包含多少个元素？如果不存在y，使得P(a, y)为T，则输出0。

问题2：如果希望谓词公式(∀x)(∀y)P(x, y)为T，则客体变量x和y的取值范围最多可以包含多少个元素？如果不存在x和y，使得P(x, y)为T，则输出0。

输入描述
客体常量a的值，a为自然数且2≤a≤10000。

输出描述
依次输出两个问题的解，用空格隔开。

样例输入
3
copy
样例输出
6666        (问题2的答案与输入无关，在此不给出答案)
*/
#include <iostream>
using namespace std;
int main()
{
	// 请在此输入您的代码
	return 0;
}
