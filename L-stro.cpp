/*
 漫步者的选择

问题描述
在一片广阔的草原上，Alex 决定进行一场旅行。他站在草原的起点 (0,0)，目标是在 15 步内准确无误地走到 (2,1) 这个位置。
每一步，他可以选择向北走一步（y 轴正方向），向南走一步（y 轴负方向），向东走一步（x 轴正方向）或向西走一步（x 轴负方向）。
在广阔的草原上，Alex 的步伐是自由的，但他想知道，有多少种不同的方式可以在 15 步内到达目的地。

请你计算 Alex 从 (0,0) 出发，经过 15 步恰好到达 (2,1) 的不同路径数量。

输入格式
无。

输出格式
输出一个整数，表示从 (0,0) 到 (2,1) 的不同路径数量。

说明
本题为填空题，只需要算出结果后，在代码中使用输出语句将结果输出即可。
*/
#include <iostream>
using namespace std;
int main()
{
	cout << 32207175 << endl;

	return 0;
}